// useEditor.ts (편집기 로직 관리자)
// useEditor는 실제 글을 쓰거나 이미지를 배치하는 **'편집 영역(Editor Interface)'**에 특화된 로직을 담습니다. Tiptap, Quill, Lexical 같은 에디터 라이브러리를 사용할 때 그 라이브러리의 인스턴스를 제어하기 위해 주로 만듭니다.

// 주요 역할
// 인스턴스 초기화: 에디터 라이브러리(예: Tiptap)의 설정값을 초기화합니다.

// 텍스트 포맷팅: 글자를 굵게(Bold) 하거나, 제목(H1)으로 바꾸는 등의 명령을 처리합니다.

// 이벤트 핸들링: 글자가 입력될 때마다 상태를 업데이트하거나, 사진을 드래그 앤 드롭할 때의 로직을 처리합니다.

// 저장 로직: 작성된 내용을 JSON이나 HTML 형식으로 변환하여 서버에 보낼 준비를 합니다.

// 보통 들어가는 내용
// editor: 에디터 인스턴스 그 자체.

// onUpdate(): 내용 변경 시 실행될 콜백.

// commands: toggleBold(), insertImage() 등 UI 버튼에서 사용할 액션들.

// wordCount: 현재 작성된 글자 수 계산.
